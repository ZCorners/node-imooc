# 安装多个依赖
cnpm install express jade moment mongoose

# 将bower安装到全局环境，可直接使用该命令
cnpm install bower -g

# boostrap
bower install bootstrap

# 由于express的版本问题需要安装body-parser
cnpm install body-parser

# 安装underscore: _
cnpm install underscore

# 生成bower.json
bower init

# 生成package.json
cnpm init

# 安装grunt
cnpm install grunt -g
cnpm install grunt-cli -g

grunt-contrib-watch监听文件增删改，重新执行任务
grunt-nodemon实时监听app.js
grunt-concurrent监控慢任务SASS LESS 等

cnpm install bcrypt --save

cnpm install cookie-parser --save
cnpm install express-session --save

cnpm install connect-mongo --save

cnpm install connect-multiparty --save
cnpm install grunt-mocha-test --save
cnpm install should --save
cnpm install mocha --save

